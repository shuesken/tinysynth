This software is, as far as I can tell, only used by me and to make this work for you you'll probably have to dig around in the code a bit.

`npm i`
`npm start`

in both `tinysynth-app` and `tinysynth-server`
